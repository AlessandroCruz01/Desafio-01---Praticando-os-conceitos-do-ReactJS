import { StyleSheet } from "react-native";
export const styles = StyleSheet.create({
	container: {
		flex: 1,
		backgroundColor: "red",
	},

	eventName: {
		color: "#fdfcfe",
		padding: 24,
		fontSize: 43,
		fontWeight: "bold",
	},
});
